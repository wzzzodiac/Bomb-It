# Current state

Updated: 2026-09-14

Playable one-player browser MVP with destructible arena, one wandering bomb-placing rival, upgrades, win/loss, and restart. Desktop and touch controls share the same scene. The built site targets GitHub Pages at `/Bomb-It/`, with compiled files checked in at the repository root for its current branch source. Validation: `npm test`, `npm run typecheck`, `npm run build`, plus one desktop and one 390×844 built-preview smoke.

No online multiplayer, sound, custom art, or persisted progress. Next useful check: confirm the public URL loads the compiled CSS, JavaScript, and canvas after the branch Pages build.
