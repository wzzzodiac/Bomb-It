{
  "name": "bomb-it",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "build": "tsc --noEmit && vite build && node scripts/sync-pages.mjs"
  },
  "dependencies": { "phaser": "^3.90.0" },
  "devDependencies": { "@types/node": "^24.3.0", "typescript": "^5.9.3", "vite": "^7.1.0" }
}
